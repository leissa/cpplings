#include <doctest/doctest.h>

// TODO: Allocate a 3x4 dynamic 2D array, fill it with row*10 + col,
// and return the element at position [2][3].

int dynamic_2d_element() { return 0; }

TEST_CASE("dynamic 2d array") { CHECK(dynamic_2d_element() == 23); }
